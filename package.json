{
  "name": "dick-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website for local development and testing",
  "scripts": {
    "start": "live-server --port=3000 --host=127.0.0.1 --no-browser",
    "dev": "live-server --port=3000 --host=127.0.0.1 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
